/*
 * DarkJSDK
 * A Tour of Booleans
 */

// JavaScript supports booleans, true or false values:

let isSunny = true;
let isRaining = false;

console.log(isSunny);   // returns true
console.log(isRaining); // returns false