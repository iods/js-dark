/*
 * DarkJSDK
 * A Tour of Variables
 */

// @TODO find more information on the `const` variable type.

const name = 'Dwight Kurt Schrute';

name = "Jim Halpert";

console.log(name); // TypeError: Assignment to a constant or read-only variable