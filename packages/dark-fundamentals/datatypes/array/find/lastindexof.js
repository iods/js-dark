/**
 * @fileoverview Default scripts for the DarkJSDK
 * Only the darkest stars burn the brightest
 * @package
 */

// does a given element exist in the array.

const users = ['Michael', 'Dwight', 'Jim', 'Kevin'];
const index = users.indexOf('Dwight');

console.log(users.lastIndexOf(1));

console.log(index); // returns 1
