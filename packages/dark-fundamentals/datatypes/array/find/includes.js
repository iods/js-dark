/*
 * DarkJSDK
 * A Tour of Arrays
 */

// Using the includes() method will return true if the array contains the specified value

const users = ['Michael', 'Dwight', 'Jim', 'Kevin'];
const isIncluded = users.includes('Kevin');

console.log(isIncluded); // returns `true`