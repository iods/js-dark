/*
 * DarkJSDK
 * A Tour of Arrays
 */

// Using the slice() method returns selected elements in an array, as a new array

const users = ['Michael', 'Dwight', 'Jim', 'Kevin'];
const newUsers = users.slice(0, 2)

console.log(newUsers); // returns ['Michael', 'Dwight']