/*
 * DarkJSDK
 * A Tour of Arrays
 */

// Using the shift() method will remove the first element of an array

const users = ['Michael', 'Dwight', 'Jim', 'Kevin'];
const first = users.shift();

console.log(first);   // removes 'Michael'
console.log(users); // ['Dwight', 'Jim', 'Kevin']