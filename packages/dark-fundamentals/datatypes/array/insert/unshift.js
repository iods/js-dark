/*
 * DarkJSDK
 * A Tour of Arrays
 */

// Using the unshift() method new items are added to the beginning of an array

const users = ['Michael', 'Dwight', 'Jim', 'Kevin'];
users.push('Creed');

console.log(users); // returns ['Creed', 'Michael', 'Dwight', 'Jim', 'Kevin']
