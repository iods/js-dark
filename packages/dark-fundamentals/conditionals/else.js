/*
 * DarkJSDK
 * A tour of Javascript Conditionals
 */

// The else conditional

/*
 * Example
 *
 */
const i = parseInt(prompt('Choose Number'));

if (i % 2 === 0) {
    console.log('This is an even number.');
} else {
    console.log('This is an odd number');
}