let programLanguages = {
     js: "JavaScript",
     cpp: "C++",
     py: "Python",
     java : "Java"
}

for (const key in programLanguages) {
     // console.log(programLanguages[key]);
     // console.log(`Keys are : ${key} & Values are : ${programLanguages[key]}`);
     // if (Object.hasOwnProperty.call(object, key)) {
     //      const element = object[key];
          
     // }
}