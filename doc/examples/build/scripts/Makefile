# Makefile - Dark SDK for JavaScript
#
# @version 0.1.4

SHELL   := /bin/bash
PROJECT := dark
VERSION := 0.1.4
ROOT    := $(shell pwd)
SRC     := $(dir $(lastword $(MAKEFILE_LIST)))
DC      := docker-compose
